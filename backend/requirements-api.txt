# Runtime deps for the deployed API only (no build-time pipeline packages:
# geopandas / shapely / pyogrio are used only by realdata/ scripts, never when
# serving requests). Local development still uses requirements.txt.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
numexpr==2.10.0
rapidfuzz==3.14.1
requests==2.32.3
python-dotenv==1.0.1
